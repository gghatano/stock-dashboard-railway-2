# Railway deployment configuration (Nixpacks)
# Environment setup only - build/start commands are in railway.json

[phases.setup]
nixPkgs = ["nodejs_22", "python312"]
aptPkgs = []

[phases.setup.env]
NIXPACKS_PYTHON_VERSION = "3.12"
NIXPACKS_NODE_VERSION = "22"
